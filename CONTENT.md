# CV content — source of truth (EN + VI)

Every string on the page comes from here. Do not write copy inline in a component.
The dev turns this into `src/content.ts` with an `en` / `vi` pair per key.

**Privacy rule:** the public page shows **city only** ("Hanoi, Vietnam"). No street address, no
phone number. Email is `work.vinh.vn@gmail.com` and is the only contact detail printed.

---

## Identity

| Field | Value |
| --- | --- |
| Display name | **Vinh Van** |
| Full legal name (small, under hero) | Vần Ngọc Vinh |
| Target role | Fullstack Developer |
| Location (public) | Hanoi, Vietnam |
| Email | work.vinh.vn@gmail.com |
| GitHub | https://github.com/acevinh |

---

## Hero

**EN**
- Eyebrow: `Fullstack Developer · Hanoi`
- Display: `Vinh Van`
- Lede: *I build product-grade web apps in NestJS and React — and I've spent the last year making AI agents a dependable part of how I ship, not a demo.*
- Actions: `Get in touch` (accent) · `Résumé (PDF)` · `GitHub`

**VI**
- Eyebrow: `Lập trình viên Fullstack · Hà Nội`
- Display: `Vinh Van`
- Lede: *Tôi xây dựng ứng dụng web mức sản phẩm bằng NestJS và React — và dành năm qua để biến AI agent thành một phần đáng tin trong quy trình làm việc, không phải thứ để trình diễn.*
- Actions: `Liên hệ` · `Tải CV (PDF)` · `GitHub`

---

## About

**EN**
> I'm a fullstack developer in Hanoi. I started as a PHP/Laravel backend apprentice in May 2025 and
> moved into a product team, where I now work across a NestJS API and a React TypeScript frontend.
>
> What I care about is the unglamorous half of shipping: knowing what a change breaks before you
> make it, and being able to prove it works afterwards. That is also why I put real effort into
> working with AI agents — I write the guardrails first, then let them move fast inside those.

**VI**
> Tôi là lập trình viên fullstack tại Hà Nội. Tôi bắt đầu với vị trí học việc backend PHP/Laravel
> từ tháng 5/2025, rồi chuyển vào team product, hiện làm cả API NestJS lẫn frontend React TypeScript.
>
> Thứ tôi quan tâm là nửa không hào nhoáng của việc ship: biết một thay đổi sẽ làm hỏng cái gì
> *trước khi* làm, và chứng minh được nó chạy đúng *sau khi* làm. Đó cũng là lý do tôi đầu tư
> nghiêm túc vào cách làm việc với AI agent — viết rào chắn trước, rồi mới để chúng chạy nhanh
> bên trong rào đó.

### Facts grid
| EN | VI |
| --- | --- |
| Based in — Hanoi, Vietnam | Nơi ở — Hà Nội, Việt Nam |
| Working since — May 2025 | Đi làm từ — 05/2025 |
| Languages — Vietnamese (native), English (professional reading & writing) | Ngôn ngữ — Tiếng Việt (bản ngữ), Tiếng Anh (đọc/viết chuyên môn) |
| Open to — Fullstack Developer roles | Đang tìm — vị trí Fullstack Developer |

---

## Experience — newest first, 4 nodes

**No total-years figure anywhere.** The timeline speaks: four steps in sixteen months.

### Node 1 — current (this one carries the only green dot)
`2026.01 — Present` · **Fullstack Developer** · XIPAT

EN bullets:
- Ship features end to end across a NestJS 11 + TypeScript API and a React 18 + TypeScript frontend.
- Work inside a product team — scoping, implementation, code review, release.
- Build the frontend against a generated API client, so a backend contract change is coordinated
  across both repos in a single change instead of breaking the client silently.
- Build embedded Shopify app surfaces with Polaris and the Admin GraphQL API.
- Run AI agents against a written process I maintain — the same rules published in `claude-skills`.

VI bullets:
- Triển khai tính năng đầu-cuối trên API NestJS 11 + TypeScript và frontend React 18 + TypeScript.
- Làm việc trong team product — chốt phạm vi, code, review, release.
- Frontend build trên API client sinh tự động, nên mọi thay đổi hợp đồng API phía backend được
  đồng bộ hai repo trong cùng một thay đổi, thay vì làm hỏng client một cách âm thầm.
- Xây giao diện Shopify embedded app bằng Polaris và Admin GraphQL API.
- Vận hành AI agent theo bộ quy trình do tôi tự viết và duy trì — chính là bộ đã public ở `claude-skills`.

#### Live product on the current role

Rendered as a link strip inside node 1. **Phrased as work *into* the product, never as authorship** —
the app shipped in 2020, years before he joined in May 2025.

| Field | Value |
| --- | --- |
| Name | Omega Google & Facebook Feed |
| Link | https://apps.shopify.com/google-shopping-feed-pro |
| Rating | 4.9 |
| Reviews | 110 |
| Live since | Nov 2020 |

- EN blurb: *A published Shopify app, live since 2020 — this is the product my day-to-day work ships into.*
- VI blurb: *Ứng dụng Shopify đã phát hành, chạy thật từ 2020 — đây là sản phẩm mà công việc hằng ngày của tôi đi vào.*

⚠️ **Two things to re-check before this CV goes anywhere.** The rating and review count are public
figures that drift, so verify them against the listing. And the store lists the publisher as
"FeedNexa (Omega)" while the CV names the employer as XIPAT — if a recruiter asks, that difference
needs a one-line answer ready.

### Node 2
`2025.11 — 2025.12` · **Probation** / *Thử việc* · XIPAT
- EN: Moved from PHP onto the TypeScript stack — NestJS on the backend, React on the frontend.
- VI: Chuyển từ PHP sang stack TypeScript — NestJS phía backend, React phía frontend.

### Node 3
`2025.07 — 2025.10` · **Backend Intern** / *Thực tập backend* · XIPAT
- EN: PHP and Laravel backend work, plus first production React + TypeScript frontend work.
- VI: Làm backend PHP/Laravel, đồng thời bắt đầu làm frontend React + TypeScript thật.

### Node 4
`2025.05 — 2025.06` · **Apprentice** / *Học việc* · XIPAT
- EN: Entered the team as an apprentice; learned the codebase and the review process.
- VI: Vào team ở vị trí học việc; học codebase và quy trình review.

---

## Education

`FPT Polytechnic` — Backend Development (PHP / Laravel)
*Cao đẳng FPT Polytechnic — Lập trình Backend (PHP / Laravel)*

---

## Skills — four groups, deliberately uneven

| Group | Items |
| --- | --- |
| **Backend** | NestJS · TypeScript · Node.js · PHP · Laravel · TypeORM · REST API design · Job queues (BullMQ) · OpenAPI |
| **Frontend** | React 18 · TypeScript · Redux Toolkit · RTK Query · Vite · Shopify Polaris · CSS architecture |
| **Data & Infra** | MySQL · MongoDB · PostgreSQL + pgvector · Redis · S3-compatible storage · Docker |
| **AI & Tooling** | Claude Code · Agent skills · Playwright · Git · Code generation |

---

## Projects — case studies (Problem → Approach → Result)

### 1. Visic — chat with your own documents
`github.com/acevinh/visic`
Tags: `NestJS 11` `React` `TypeScript` `PostgreSQL` `pgvector` `SSE` `RAG`

- **Problem (EN):** An LLM answer you cannot trace back to a source is useless when the source is
  your own documents — you end up verifying every sentence by hand.
- **Approach (EN):** A NestJS 11 + TypeORM API with JWT access/refresh (refresh tokens stored
  hashed). Uploaded documents are parsed, chunked and embedded into PostgreSQL with pgvector at 768
  dimensions. A question runs top-k retrieval, and only the retrieved chunks are allowed into the
  prompt. Tokens stream back over SSE to a React + TypeScript client that renders citations inline.
  The model sits behind an `LlmProvider` interface so it can be swapped without touching the app.
- **Result (EN):** A working end-to-end MVP — sign in, upload, embed, ask, get an answer with
  citations — running entirely on a free-tier model, at zero operating cost.

- **Vấn đề (VI):** Một câu trả lời của LLM mà không truy ngược được về nguồn thì vô dụng khi nguồn
  chính là tài liệu của bạn — cuối cùng vẫn phải kiểm tay từng câu.
- **Cách làm (VI):** API NestJS 11 + TypeORM, JWT access/refresh (refresh token lưu dạng băm).
  Tài liệu upload được parse, cắt chunk và embed vào PostgreSQL + pgvector 768 chiều. Mỗi câu hỏi
  chạy truy hồi top-k, và chỉ những chunk truy hồi được mới được đưa vào prompt. Token stream về
  client React + TypeScript qua SSE, hiển thị trích dẫn ngay trong câu trả lời. Model nằm sau
  interface `LlmProvider` nên thay được mà không đụng vào ứng dụng.
- **Kết quả (VI):** MVP chạy thông đầu-cuối — đăng nhập, upload, embed, hỏi, nhận câu trả lời kèm
  trích dẫn — chạy hoàn toàn trên model free-tier, chi phí vận hành bằng 0.

### 2. claude-skills — process rules for AI agents
`github.com/acevinh/claude-skills`
Tags: `Claude Code` `Agent skills` `Developer tooling` `Open source`

- **Problem (EN):** Most agent failures I hit were not reasoning failures. They were process
  failures: a one-line fix quietly becoming a refactor, a convention cited that the repo never had,
  and "done" claimed on a check that never ran.
- **Approach (EN):** Four skills, each encoding one habit. Route by file count before editing.
  Review a diff only against rule docs opened in this session — never a remembered rule. Never claim
  a gate passed without pasting its output. Persist every test case to a ledger the next session
  can re-run, so test coverage compounds instead of resetting.
- **Result (EN):** Published as an open-source pack — extracted from daily use on a production
  codebase and rewritten to carry no project specifics.

- **Vấn đề (VI):** Phần lớn lỗi tôi gặp với agent không phải lỗi suy luận, mà là lỗi quy trình:
  một sửa đổi một dòng lặng lẽ phình thành refactor, trích dẫn một quy ước mà repo chưa từng có,
  và báo "xong" cho một bước kiểm tra chưa từng chạy.
- **Cách làm (VI):** Bốn skill, mỗi cái đóng gói một thói quen. Định tuyến theo số file trước khi
  sửa. Review diff chỉ dựa trên tài liệu quy ước đã mở trong phiên đó — không bao giờ dựa vào trí
  nhớ. Không báo pass nếu chưa dán output thật. Ghi mọi test case vào một ledger để phiên sau chạy
  lại được, nhờ đó độ phủ test tích luỹ thay vì mất trắng.
- **Kết quả (VI):** Đã public dạng open-source — rút ra từ việc dùng hằng ngày trên codebase thật
  và viết lại để không còn dính đặc thù dự án nào.

---

## Working with AI — its own section, not a skill chip

**EN**
> I use AI agents every working day, and the lesson has been that the model is rarely the
> bottleneck. The process is.
>
> Left unconstrained, an agent will widen a one-line fix into a refactor, cite a convention the repo
> never had, and report success on a check it never ran. So I write the constraints down instead of
> hoping. The rules I run enforce four things: route by file count before editing; review a diff
> only against rule docs opened in that session; never claim a gate passed without pasting its
> output; and leave every test case in a file the next session can re-run.
>
> What I'd bring to a team is not "I use AI" — it's a working method for using it without losing
> control of the codebase.

**VI**
> Tôi dùng AI agent mỗi ngày làm việc, và bài học rút ra là: model hiếm khi là nút thắt. Quy trình mới là.
>
> Nếu không ràng buộc, agent sẽ biến một sửa đổi một dòng thành refactor, trích dẫn một quy ước mà
> repo chưa từng có, và báo thành công cho một bước kiểm tra nó chưa từng chạy. Nên tôi viết ràng
> buộc ra thay vì hy vọng. Bộ quy tắc tôi dùng bắt buộc bốn điều: định tuyến theo số file trước khi
> sửa; review diff chỉ dựa trên tài liệu quy ước đã mở trong phiên đó; không báo pass nếu chưa dán
> output thật; và để lại mọi test case trong một file mà phiên sau chạy lại được.
>
> Thứ tôi mang tới cho team không phải là "tôi biết dùng AI" — mà là một phương pháp làm việc để
> dùng nó mà không mất kiểm soát codebase.

---

## Certifications — all three, two publicly verifiable

| Credential | Issuer | Date | Verify |
| --- | --- | --- | --- |
| Claude Code in Action | Anthropic Education | Apr 2026 | https://verify.skilljar.com/c/quhis252443d |
| Introduction to Agent Skills | Anthropic Education | Apr 2026 | https://verify.skilljar.com/c/ytamcsc3sxu6 |
| Developing Apps for Shopify | Shopify Academy | Apr 2026 | — |

Certificates are issued to **Vinh Ace**; note that on the card so the name difference is not a surprise.

---

## Contact

**EN:** *Open to Fullstack Developer roles. The fastest way to reach me is email.*
**VI:** *Đang tìm vị trí Fullstack Developer. Cách nhanh nhất để liên hệ là email.*

- `work.vinh.vn@gmail.com` — with a copy button
- `github.com/acevinh`

---

## Footer

**EN:** Built with React, TypeScript and Vite. No tracking, no cookies. — `Source on GitHub`
**VI:** Xây bằng React, TypeScript và Vite. Không theo dõi, không cookie. — `Mã nguồn trên GitHub`
